var v1,v2;
{
    var back1;
    var back2;

    assertEquals("V1", v1);
    assertEquals("V2", v2);

    import "./shareRootParams.include.wit" {"a1":"A1","a2":"A2"};

    assertEquals("BACK1", back1);
    assertEquals("BACK2", back2);
}
